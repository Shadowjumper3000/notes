# Image Segmentation
Image segmentation is the process of partitioning an image into multiple segments or sets of pixels. Semantic segmentation labels each pixel with a category, while instance segmentation distinguishes between individual objects.
